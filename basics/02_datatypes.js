"use strict" // iska matlab treat all code as new version, agra older version wala code likhe to error throw karega.

//alert("hello....") //browser par work karega not in node js

console.log(3 + 3)
